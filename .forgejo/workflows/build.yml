name: build-springbootweb
on: [push]
jobs:
  build-springbootweb:
    runs-on: codeberg-small-lazy
    container:
       image: registry.opensuse.org/opensuse/bci/nodejs # 25.06.2026
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # 7.0.0
        with:
          persist-credentials: false
      - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # 6.4.0 
        with:
          node-version: '26'
      - name:Install libatomic for setup-java action
        run: |
           sudo zypper -y libatomic1
      - uses: actions/setup-java@ad2b38190b15e4d6bdf0c97fb4fca8412226d287 # 5.3.0
        with:
          java-version: '25'
          distribution: 'temurin'
      - name: Build with Gradle
        run: |
           cd backend
           ./gradlew dependencyUpdates -DoutputFormatter=json,xml,html,txt,plain --refresh-dependencies
           ./gradlew clean build
